'''
    config.py
 
    In a secure setup, we would place this file outside the web server's
    root directory, so it would not be possible for clients to obtain this
    file over the network.
'''
database = 'shim'
user = 'shim'
password = ''
